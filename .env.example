# MCP Server Configuration
# Copy this file to .env and customize for your environment

# Application Settings
APP_DEBUG=false
APP_VERSION=1.0.0
DOCKER_IMAGE=davidsmith3/mcp-server

# Server display name shown in Claude Desktop
MCP_SERVER_NAME=mcp-server

# Colon-separated paths to discover controllers
# Default: controllers
# Examples:
#   MCP_CONTROLLER_PATHS=controllers
#   MCP_CONTROLLER_PATHS=controllers:app/Http/Controllers
#   MCP_CONTROLLER_PATHS=controllers1:controllers2:controllers3
MCP_CONTROLLER_PATHS=controllers

# Directory for session storage (Docker container path)
# Default: /app/storage/mcp-sessions
MCP_SESSIONS_DIR=/app/storage/mcp-sessions

# API Keys (controller-specific)
# Uncomment and add any API keys your controllers need
# API_KEY=your_api_key_here
# OPENWEATHER_API_KEY=your_weather_key
# GITHUB_TOKEN=your_github_token
